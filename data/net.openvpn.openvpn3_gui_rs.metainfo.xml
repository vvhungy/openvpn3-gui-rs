<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>net.openvpn.openvpn3_gui_rs</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>
  <name>OpenVPN3 GUI</name>
  <summary>System tray indicator for OpenVPN3 VPN tunnels</summary>

  <description>
    <p>
      A system tray application for managing OpenVPN3 VPN tunnels on Linux.
      Written in Rust with GTK4 for better performance and lower memory usage
      compared to the Python version.
    </p>
    <p>Features:</p>
    <ul>
      <li>System tray icon with dynamic status (idle, connecting, active, paused, error)</li>
      <li>VPN configuration management (import, remove)</li>
      <li>Session management (connect, disconnect, pause, resume, restart, reconnect)</li>
      <li>Desktop notifications for status changes (deduplicated, grouped per connection)</li>
      <li>Secure credential storage via Secret Service (oo7)</li>
      <li>Launch on login via XDG autostart (toggle in Preferences)</li>
      <li>Auto-connect on startup (most-recent or specific profile)</li>
      <li>Reconnect prompt on unexpected session drop</li>
      <li>Username/password, challenge/OTP, and browser-redirect authentication</li>
      <li>Tabbed session log viewer with live log streaming, per-tab search and log-level filter, filter-aware copy, and persistent window size</li>
      <li>Tabbed Preferences dialog (General / Security / Routing) for startup, notifications, intervals, timeout, stall detection, kill-switch, split-tunneling bypass CIDR editor with per-entry enable/disable, and credentials</li>
      <li>Configurable connection timeout with notification</li>
      <li>Connection stall detection — flags an idle tunnel in tray menu and icon</li>
      <li>Kill-switch — block all non-VPN traffic via nftables when a tunnel drops (requires the privileged helper package); always-visible state row (lock/unlock) in tray menu; lock indicator in session label when active; notifications on rule apply (persistent) and removal; helper-missing notification and Preferences hint; quit confirmation warns before removing rules</li>
      <li>Split tunneling — exempt specific networks from the VPN tunnel so they route over the local connection (requires the privileged helper package); configure bypass CIDRs in the Preferences Routing tab with per-entry enable/disable checkboxes; always-visible state row in tray menu; notifications on route apply and failure; symmetric IPv4/IPv6 routing with MSS clamping</li>
      <li>First-run help notification when the OpenVPN3 backend is not installed or reachable</li>
      <li>Automatic recovery when the OpenVPN3 service restarts</li>
      <li>Multiple VPN configuration support</li>
    </ul>
  </description>

  <launchable type="desktop-id">net.openvpn.openvpn3_gui_rs.desktop</launchable>

  <url type="homepage">https://github.com/vvhungy/openvpn3-gui-rs</url>
  <url type="bugtracker">https://github.com/vvhungy/openvpn3-gui-rs/issues</url>
  <url type="vcs-browser">https://github.com/vvhungy/openvpn3-gui-rs</url>

  <provides>
    <binary>openvpn3-gui-rs</binary>
  </provides>

  <requires>
    <display_length compare="ge">360</display_length>
  </requires>

  <recommends>
    <control>pointing</control>
    <control>keyboard</control>
  </recommends>

  <content_rating type="oars-1.1" />

  <releases>
    <release version="0.3.7" date="2026-05-28">
      <description>
        <p>Sprint 30: auto-reconnect, log export, statistics dialog, and split-tunnel PoC validation</p>
        <ul>
          <li>Auto-reconnect after unexpected disconnect — when enabled, the app waits a configurable delay (5–300s, default 30s) then reconnects automatically. Falls through to the existing reconnect notification if the attempt fails.</li>
          <li>Auto-reconnect on stall — a connected session that stops moving bytes past the stall threshold is now also treated as an unexpected drop when auto-reconnect is on. Cooldown window prevents reconnect loops against a permanently dead server.</li>
          <li>Log export to file — new Export button in the log viewer toolbar writes the active tab's filtered entries (respecting search and level filter) as a plain-text file for bug reports or offline analysis.</li>
          <li>Connection statistics dialog — new per-session Statistics menu item opens a live-refreshing window showing config name, status, connected-since timestamp, duration, tunnel interface, and byte counters. Auto-closes when the session disconnects.</li>
          <li>Split-tunnel PoC script — conntrack/MTU probes now target a known-reachable control host so they no longer SKIP on networks that filter the bypass destination; new test-with-killswitch command validates bypass traffic through the kill-switch; test-resume automated via D-Bus.</li>
        </ul>
      </description>
    </release>
    <release version="0.3.6" date="2026-05-21">
      <description>
        <p>Sprint 29: dialog lifecycle hygiene, cold-start auth, and About styling</p>
        <ul>
          <li>Dialog singleton registry — Preferences, View Logs, About, and other windows now reuse a single instance instead of stacking duplicates on repeated tray clicks</li>
          <li>Modal funnel — when a confirmation dialog is open, any tray action raises the modal instead of opening a new window, preventing dead-lock behind an invisible grab</li>
          <li>Preferences is now non-modal so tray notifications remain visible while configuring settings</li>
          <li>Cold-start authentication — sessions already waiting for credentials when the GUI starts now prompt the user automatically instead of showing "Authentication required" with no dialog</li>
          <li>Unified auth dialog — all authentication flows (credentials, challenge/OTP) now route through a single 3-field dialog (username, password, authentication code) with keyring pre-fill</li>
          <li>About dialog — replaced GtkAboutDialog with libadwaita AboutWindow, fixing the blue headerbar artifact on GNOME</li>
        </ul>
      </description>
    </release>
    <release version="0.3.5" date="2026-05-20">
      <description>
        <p>Sprint 28: service-lifecycle robustness and per-CIDR split-tunneling status</p>
        <ul>
          <li>Connect: retry NewTunnel with exponential backoff (500 ms / 1 s / 2 s) so the first connect after a fresh login no longer fails when the openvpn3-sessions service has not yet been D-Bus activated</li>
          <li>Watcher: recover gracefully when the openvpn3-sessions service is killed — clear stale tray sessions, drop kill-switch and bypass routes at the helper, and notify the user instead of leaving zombie entries that silently reject Disconnect/Resume/Restart</li>
          <li>Split tunneling: per-CIDR success/failure reporting — tray label distinguishes "N networks" (all applied), "N active, M failed" (partial), and "Apply failed" (system-wide); a persistent notification lists up to five failing CIDRs so the user knows exactly which subnets did not route outside the tunnel</li>
          <li>Helper: ApplyBypassRoutes now returns per-entry granularity instead of failing the whole batch on the first bad CIDR; MIN_HELPER_VERSION raised to 0.3.5 to match the wire-incompatible signature change</li>
        </ul>
      </description>
    </release>
    <release version="0.3.4" date="2026-05-20">
      <description>
        <p>Sprint 27: split-tunneling DNS-leak documentation, build-step assertions, resume-auth diagnostics</p>
        <ul>
          <li>Docs: surface DNS-leak caveat for split tunneling in README and the Routing preferences tab — bypass routes destination IPs only, query names still hit the system resolver</li>
          <li>Docs: full code-path + failure-mode analysis of the DNS resolver path appended to docs/split-tunneling.md</li>
          <li>Diagnostics: info-level logs for resume-after-pause re-auth dispatch (session_ops, status_handler) to disambiguate the long-pause re-credential flow</li>
          <li>Build: Makefile dist targets (deb, deb-helper, rpm, rpm-helper) now assert artifact presence with ls -la after each cargo invocation — mirrors the release.yml post-conditions</li>
        </ul>
      </description>
    </release>
    <release version="0.3.3" date="2026-05-19">
      <description>
        <p>Sprint 26: per-entry bypass toggle, AUR helper packaging, close-out</p>
        <ul>
          <li>Split tunneling: per-entry enable/disable checkboxes in the Routing tab — suspend a bypass CIDR without losing the row</li>
          <li>Packaging: AUR PKGBUILD for the privileged kill-switch helper</li>
          <li>Packaging: removed unreferenced killswitch SVG icons from DEB/RPM assets</li>
          <li>Docs: kill-switch and split-tunneling design docs refreshed to match current code</li>
          <li>Failure modes: silent-failure tail closed as won't fix with recorded rationale</li>
        </ul>
      </description>
    </release>
    <release version="0.3.2" date="2026-05-19">
      <description>
        <p>Launch on login, notification improvements, release hardening</p>
        <ul>
          <li>Launch on login — XDG autostart toggle in Preferences General tab</li>
          <li>Split tunneling: per-entry enable/disable checkboxes in the Routing tab — suspend a bypass CIDR without losing the row</li>
          <li>Notifications: surface silent failures on kill-switch re-apply and menu actions</li>
          <li>Release workflow: per-step artifact assertions catch silent build failures early</li>
          <li>bump-version: awk-based prepend preserves all prior release entries</li>
          <li>Packaging: AUR PKGBUILD for the privileged kill-switch helper</li>
        </ul>
      </description>
    </release>
    <release version="0.3.1" date="2026-05-18">
      <description>
        <p>Patch release: CI packaging fix</p>
        <ul>
          <li>Release workflow: pre-create RPM output directory so cargo-generate-rpm produces correctly-named .rpm files (fixes v0.3.0 release shipping without RPMs)</li>
        </ul>
      </description>
    </release>
    <release version="0.3.0" date="2026-05-18">
      <description>
        <p>Split-tunneling bypass, kill-switch hardening, and production readiness</p>
        <ul>
          <li>Split-tunneling: exempt specific networks from VPN via bypass CIDR editor (Preferences Routing tab)</li>
          <li>Kill-switch: bypass tray indicator, lock icon in session label, persistent notifications, quit confirmation</li>
          <li>Logs viewer: per-tab search and log-level filter, filter-aware copy, persistent window size</li>
          <li>GSettings schema migration smoke test for safe upgrades</li>
          <li>Connect-time failure-mode audit — silent errors now surface as notifications</li>
          <li>RPM packaging fixes (asset paths, directory entries)</li>
          <li>Version management: single source of truth from Cargo.toml, make bump-version target</li>
        </ul>
      </description>
    </release>
  </releases>

  <keywords>
    <keyword>vpn</keyword>
    <keyword>openvpn</keyword>
    <keyword>network</keyword>
    <keyword>tray</keyword>
    <keyword>indicator</keyword>
    <keyword>security</keyword>
    <keyword>privacy</keyword>
  </keywords>

  <categories>
    <category>Network</category>
    <category>Security</category>
  </categories>
</component>
